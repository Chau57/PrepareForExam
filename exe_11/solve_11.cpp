#include <bits/stdc++.h>

const int N = (int)1e5;

int n, m, k;
std::pair<int, int> a[N];

int main(void) {
    std::ios_base::sync_with_stdio(0);
    std::cin.tie(0);
    std::cin >> n >> m >> k;
    for (int i = 1; i <= n; ++i) std::cin >> a[i].first >> a[i].second;

    
    return 0;
}