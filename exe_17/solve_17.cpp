/*
    1. Reverse(1, i)
    -> at 1, back to i
    -> at j, Reverse(1, j) then Reverse(1, i) (plus Reverse(1, j) if need
*/

#include <bits/stdc++.h>

const int N = (int)1e5 + 3;

int n;
int a[N];

int main(void) {
    std::ios_base::sync_with_stdio(0);
    std::cin.tie(0);
    std::cin >> n >> m >> k;
    for (int i = 1; i <= n; ++i) std::cin >> a[i];

    in
    return 0;
}